import { Provider } from '@loopback/core';
import { Cognito, VerifyFunction } from '../../types';
declare const CognitoStrategy: any;
export interface CognitoAuthStrategyFactory {
    (options: Cognito.StrategyOptions, verifierPassed?: VerifyFunction.CognitoAuthFn): typeof CognitoStrategy;
}
export declare class CognitoStrategyFactoryProvider implements Provider<CognitoAuthStrategyFactory> {
    private readonly verifierCognito;
    constructor(verifierCognito: VerifyFunction.CognitoAuthFn);
    value(): CognitoAuthStrategyFactory;
    getCognitoAuthStrategyVerifier(options: Cognito.StrategyOptions, verifierPassed?: VerifyFunction.CognitoAuthFn): typeof CognitoStrategy;
    private _setupProxy;
}
export {};
