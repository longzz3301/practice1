import { Provider } from '@loopback/core';
import { Strategy, SamlConfig } from '@node-saml/passport-saml';
import { VerifyFunction } from '../../types';
export interface SamlStrategyFactory {
    (options: SamlConfig, verifierPassed?: VerifyFunction.SamlFn): Strategy;
}
export declare class SamlStrategyFactoryProvider implements Provider<SamlStrategyFactory> {
    private readonly verifierSaml;
    constructor(verifierSaml: VerifyFunction.SamlFn);
    value(): SamlStrategyFactory;
    getSamlStrategyVerifier(options: SamlConfig, verifierPassed?: VerifyFunction.SamlFn): Strategy;
    private _setupProxy;
}
