import { Next, Provider } from '@loopback/core';
import { Middleware, MiddlewareContext } from '@loopback/rest';
import { AuthenticateFn, IAuthClient } from '../types';
export declare class ClientAuthenticationMiddlewareProvider implements Provider<Middleware> {
    private authenticateClient;
    constructor(authenticateClient: AuthenticateFn<IAuthClient | undefined>);
    value(): (ctx: MiddlewareContext, next: Next) => Promise<import("@loopback/core").NonVoid>;
}
