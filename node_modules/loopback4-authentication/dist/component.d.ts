import { Binding, Component, ProviderMap } from '@loopback/core';
import { AuthenticationConfig } from './types';
export declare class AuthenticationComponent implements Component {
    private readonly config?;
    constructor(config?: AuthenticationConfig | undefined);
    providers?: ProviderMap;
    bindings?: Binding[];
}
